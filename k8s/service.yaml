apiVersion: v1
kind: Service
metadata:
  name: growbot-sensor-exporter
  namespace: monitoring
  labels:
    app: growbot-sensor-exporter
spec:
  type: ClusterIP
  ports:
    - name: metrics
      port: 9101
      targetPort: metrics
      protocol: TCP
  selector:
    app: growbot-sensor-exporter
